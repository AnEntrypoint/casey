node_modules/
data/
*.log

# operator secrets — scaffolded by `casey init`, never committed
.env

# editor / agent session state
.claude/

# gm-plugkit runtime working state (machine-local tooling, not casey source)
.gm/

# this repo has no lockfile committed yet; ignore stray npm lock
package-lock.json
.gm/pw-sock-*/
.gm/browser-profile/
.gm/browser-profile-*/
