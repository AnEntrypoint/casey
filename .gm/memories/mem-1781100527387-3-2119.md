---
key: mem-1781100527387-3-2119
ns: default
created: 1781100530931
updated: 1781100530931
---

casey CASE GUARDRAILS / XSTATE / HEALTH SWEEP (commit b1a425b, AnEntrypoint/casey main). xstate@5.32.0 added. src/case-machine.js: buildCaseMachine(wfGraph) -> xstate v5 machine (stage=state, legal target=event GO_<TARGET>, requires_role on state meta); canTransition(machine,from,to,role) + nextStates(machine,from,role) derive legality FROM machine.config (no live actor). case-store.js init() builds this._machine from _wf; _validateTransition delegates to canTransition, availableTransitions to nextStates. xstate is VALIDATION-ONLY -- thatcher remains the sole writer of persisted case.status (no dual source of truth, no per-case actor). src/case-health.js: PURE classifyCaseHealth(caseRow, now, thresholds)->[{breach,since_ms,detail}] for stale/stuck/unanswered_handoff/abandoned_intake (open) + never_closed (resolved); closed/non-open -> []. DEFAULT_THRESHOLDS (staleMs 48h, handoffMs 4h, abandonMs 24h, neverClosedMs 7d, stageMaxDwellMs per stage). ms() normalizes ISO last_event_at vs unix-seconds created_at. healthTag(b)='health:'+b. src/case-sweep.js: sweepCases(store,now,thresholds) recomputes desired health:* set, reconciles via updateCaseQuiet (NO last_event_at touch) + appendEvent touch:false, sets new / CLEARS resolved, ONE observation per newly-entered breach, preserves non-health tags. CRITICAL INVARIANT: the sweep must never touch last_event_at (it is the staleness signal) -- hence the new store primitives updateCaseQuiet and the appendEvent {touch:false} option. casey.js: runSweepOnce(), startSweep(intervalMs default 15min, unref'd, restart-safe), stopSweep() clears _sweepTimer, start() enables by default (opt-out sweepIntervalMs:0), stop() clears it; sweep errors caught never crash loop. case-tools case_health(id). dashboard GET /api/attention + healthBadges() PAGE chips (plain-language, never raw health: tags) -- browser-witnessed (browser-22: chipCount 3, attentionCount 1). GOTCHA learned: spinning a 2nd createCasey in a test swaps the global case-store singleton (setCaseStore) and corrupts later tests -- test timer lifecycle on a bare new Casey() instead. All tests green.
