---
key: mem-fdc2ee71636ae485-1119
ns: default
created: 1782759737568
updated: 1782759737568
---

gm-method (witnessed): when a user demands a genuinely-understanding redesign of a fuzzy NL surface and explicitly asks to fan out subagents, author a Workflow with the canonical Map->Design->adversarial-Verify->Synthesize shape: parallel readers map each real surface (with a schema forcing file:line witnesses), per-dimension designers ground in that map, default-refute verifiers catch the holes, and one synthesizer folds every verifier fix into a dependency-ordered apply-ready plan plus the reference data (here a domain alias table) and a test matrix. The synthesizer's output is directly implementable -- but STILL re-verify each piece against real input as you land it: the workflow's design had a 14/15 then 16/17 intent matrix until the verb-vs-noun and clear-enquiry-lead corner cases were tightened by live exec_js runs. The workflow does the breadth; your inline witness loop does the last-mile correctness. Also: a domain NL classifier needs a 'safe default' veto (here: livestock-bearing message defaults to report in a disease service) so an enquiry recogniser never swallows the primary intake intent.
