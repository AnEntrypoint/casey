---
key: mem-44e62d07e8fe0fbe-805
ns: default
created: 1783420767124
updated: 1783420767124
---

## Resolved mutable: mut-mineRows-vs-workload-personOwner-identity

workload.js personOwner() (line ~39-42): function personOwner(caseRow) { const a = String(caseRow?.assignee||'').trim(); return a && a!=='agent' ? a : '' } -- reads case.assignee (the OPERATOR who claimed the case in the dashboard). case-tools.js mineRows() (line ~514-524): matches ctx.author (the channel/contact identity) against c.external_id. CONFIRMED these are deliberately DISTINCT identity axes by design, not a naming collision: assignee = which dashboard operator owns/claimed the case (workload/Team panel scope), external_id = which field-worker/contact the case belongs to (case_mine/case_today worker-enquiry scope, per AGENTS.md's row_access owner-field model). Not a bug -- correctly separated concerns. No finding here.
