---
key: mem-63eda0c01958682c-2239
ns: default
created: 1780949072829
updated: 1780949072829
---

casey engineering-DNA structural pass (commit f7f8284, AnEntrypoint/casey main). 10-agent design+critique workflow (w3v0n9ajb) applying synthesized-engineering-dna; integrated critique-corrected code. LOAD-BEARING FIX (concurrency bug the crucible exposed): inbound dedup+append ran OUTSIDE the per-conversation lock, so DUP concurrent identical messages could record up to DUP inbound rows. Added CaseStore.recordInbound on a shared _withLock(key,fn) primitive (refactored findOrCreateCase to use it too) so dedup-check+append are atomic -> duplicate inbounds structurally unrepresentable. gateway-hooks.js now calls store.recordInbound (returns null on dup). CORRECTNESS: findOpenCase rewritten -- thatcher where is equality-only AND thatcher IGNORES orderBy/order (sorts only via options.sort/spec.list.defaultSort), so it now queries each open workflow stage with equality where and picks max created_at in JS (was: list 25 + JS filter status!=closed, could miss an open case behind >25 closed). HONEST-INTERFACE: removed databasePath option (thatcher ignores it; getDatabase primed argless from cwd at index.js:148; database lives at cwd/data/app.db, not configurable). PERF: _count cap 100000->50000 (Route A, no thatcher fork). Empirical (.gm/count-bench N=8000): cap is a worst-case ceiling guard NOT a per-poll win -- both ~40ms because materialization is the cost; below the cap they are equal. thatcher has internal query-engine.js count() (SELECT COUNT*) but NOT on the public Thatcher instance; forking REJECTED: node_modules edit is npm-clobberable AND sibling C:/dev/thatcher is a BROKEN 1.0.11 busybase fork (index.js imports deleted lib/query-engine*.js). ADVERSARIAL: dashboard PATCH/transition/note/reply validate via str() helper + AUTONOMY/PRIORITY enum Sets, oversized->413, unknown transition->400 with allowed list, BUT transition allows no-op same-stage (availableTransitions excludes current stage yet transition() no-ops it -- gating it would 400 a valid no-op). test.js 45 green incl CRUCIBLE (40 convos x4 dup concurrent + partial send-failure + volume), adversarial-validation, countCases-exactness. NO client PAGE edits this pass (all server-side) so no browser-witness needed. thatcher/freddie NOT modified.
