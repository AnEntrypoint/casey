---
key: mem-54e3a7322d682520-1247
ns: default
created: 1783455572387
updated: 1783455572387
---

## Resolved mutable: mut-1783455469177

Read C:/dev/rs-plugkit/src/memory_md.rs:869-918 export_flat_json(): writes EXPORT_MARKER='.flat-export-done' into md_dir once a pass has deferred==0 && exported==pending.len(), and host_read(&marker).is_some() short-circuits ALL future calls permanently (line 875-877), regardless of remaining un-exported legacy entries. .gm/memories/.flat-export-done exists (mtime Jul 7 21:41) while 596/1166 .gm/disciplines/default/mem-<epoch>-<idx>-<len>.json legacy files remain unconverted -- the marker was written prematurely (likely EXPORT_BATCH_MAX=200-per-pass cap combined with a KV-query snapshot that didn't see all entries), permanently gating off further export for this namespace. sync_index() (same file, line 537) is a SEPARATE mechanism that only rekeys/reindexes existing .md files by content-hash and never reads the legacy json at all -- confirmed by watching memory_md_sync_partial events during this session (deferred counter dropping 899->466, rekeyed:0 always) with zero effect on legacy json count (stayed 1166) or md count (stayed 1169) across 50+ instruction dispatches. Root cause: two independent code paths, marker gates the only one that reads legacy json, and it fired 'done' incorrectly.
