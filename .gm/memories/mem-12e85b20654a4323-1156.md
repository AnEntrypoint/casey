---
key: mem-12e85b20654a4323-1156
ns: default
created: 1783456921128
updated: 1783456921128
---

## Resolved mutable: mut-1783456165198

Same finding as mut-rssearch-vectors-db-malformed (already resolved). Root cause confirmed and fixed: stale gm.db-journal at C:/Users/user/.gm-tools/wasi-fs/5603ecea8fc4d979/gm.db-journal caused libsql-in-wasm to report 'database disk image is malformed' on every ensure_schema call. Verified the underlying gm.db data was intact via node:sqlite on a copy (journal replayed cleanly, all 11 expected tables present including memories_md_meta/memories_md_files/rssearch_vectors). Backed up real db+journal to scratchpad, replaced live gm.db with the verified rolled-back copy, deleted the stale journal, restarted the watcher (taskkill 14632 -> supervisor respawned fresh pid). Post-fix: zero new 'malformed' log lines across 100+ subsequent instruction dispatches, memory_md_rekeyed_batch resumed firing and drove deferred count from 977 to 0 (sync_index converged, digest stored), and codesearch's vector_hits (previously erroring 'exec rc=11 msg=database disk image is malformed') now returns real fusion-mode results with populated vector_hits array -- confirms the DB is genuinely healthy again, not just quieter.
