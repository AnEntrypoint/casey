---
key: mem-b2596d9df6a4fb2b-815
ns: default
created: 1790085151078
updated: 1790085151078
---

gm's git_commit verb IGNORES the paths field in this build (runner 0.1.144 / gm plugin 0.1.1332) -- confirmed again 2026-09-22: a git_commit with paths limited to four files committed all eleven dirty paths in a shared worktree, sweeping another agent's in-progress work. Recovery that loses nothing: git reset --soft HEAD~1 then git reset (mixed) restores the exact pre-commit worktree+index state, nothing was pushed. For index-precise staging in a shared worktree (only SOME hunks of a file that another agent is also editing), dump git diff -- <file>, split it on @@ boundaries, keep the hunks whose text is yours, and git apply --cached --recount that filtered patch; then git add the whole-file paths and git commit with NO pathspec so it commits exactly the index. Verify with git show --stat before pushing.
