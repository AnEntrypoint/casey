---
key: mem-6dab7da552be8b5e-1723
ns: default
created: 1783016492020
updated: 1783016492020
---

gm-method (witnessed): interpreting a vague/jargon-laden ultracode directive ('maximize orthogonal teleology') is best resolved by recall -- a prior session's memorized lesson ('when a user demands a genuinely-understanding redesign... and explicitly asks to fan out subagents, author a Workflow with the canonical Map->Design->adversarial-Verify->Synthesize shape') directly named the pattern to apply, turning an ambiguous phrase into a concrete, executable plan (six orthogonal review axes, each independently mapped then adversarially verified, synthesized into a ranked punch list). Also witnessed: a broad post-change review workflow (map N independent axes, verify each, synthesize) found a REAL high-severity PII leak (a tool handler with no ownership/role check returning full contact data for any id, while a sibling tool correctly scoped the same data) that had shipped silently for an unknown period -- the value of orthogonal/independent axes is that a security sweep run purely for its own sake caught a bug no functional test would have exercised (the leak only manifests when an UNRELATED party's id is queried, not the happy path any single-threaded manual test would naturally try). Also witnessed: removing a fallback/safety-net mechanism can create a SECOND-ORDER observability gap -- code that used to signal 'something went wrong' via visible fallback text now sends nothing, so an existing operator-facing tag/queue mechanism (built assuming SOME text always went out) silently stops covering a whole class of failure; the fix reused the existing tag/queue plumbing (widened its trigger condition) rather than building new UI, since the visibility surface already existed and only needed a wider net.
