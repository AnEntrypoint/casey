---
key: mem-b8d6536236d4f8d9-1105
ns: default
created: 1783454379548
updated: 1783454379548
---

casey src/case-store.js caveats (drained from AGENTS.md source map): (1) SQLITE_BUSY retry proxy on the t getter -- list/get/create/update/remove retry with bounded linear backoff so a concurrent agent read against a live write never surfaces a 'database is locked' turn error. (2) learnOperatorActivity/listOperatorIdentities maintain the operator_identity entity: durable per-operator working-area history, dashboard-attributed actions only, best-effort and never throws into the caller. (3) No server-side geocoding of any kind -- case.lat/lon are written ONLY by the agent's own case_report call. (4) mergeReport uses thatcher's optimistic-lock guard (c._version + updateCase opts.expectedVersion) to detect a dashboard-PATCH racing an agent-turn report merge on the same case: a genuine conflict retries the merge once against the freshly re-read row so both sides' edits survive, neither write silently lost. (5) appendReportField (photos/audio/sites) appends rather than overwrites -- a worker sending a second photo/voice note/site note across a conversation must never have it silently discarded.
