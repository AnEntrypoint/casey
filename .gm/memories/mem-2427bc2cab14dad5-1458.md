---
key: mem-2427bc2cab14dad5-1458
ns: default
created: 1781868235539
updated: 1781868235539
---

casey QUALITY-MAXIMISER PASS 5 (sha 7122e66, main). 20 confirmed / 33 rejected from per-surface + cross-cutting + adversarial audit. KEY FIXES: Security: dashboard token no longer accepted via query param (rank 23, Bearer/x-casey-token only); WHATSAPP_APP_SECRET now hard-fails startup if WhatsApp creds present (rank 24); mergeTag unexported from gateway-hooks (rank 1). Data integrity: mergeReport validates against REPORT_KEYS set before acquiring lock (rank 4); markReportFieldsIfEmpty logs JSON parse failures (rank 13); mergeCases atomically closes source before tagging merged (rank 22). Reliability: case-runtime distinguishes closed vs uninitialised store errors (rank 12); onTransition hook failure appended as observation event (rank 9); sweep aborts at 100+ errors to avoid runaway (rank 14); createDashboard throws on null store (rank 21); PATCH /api/cases/:id blocks all content edits in observe-mode (rank 19); all dashboard mutation errors return 500 not 400 (rank 18); POST /note wrapped in try-catch (rank 17). UX/CLI: show command displays case id not external_id (rank 15); up validates no-channels condition (rank 6); cases validates --status flag (rank 5). Input safety: dashboard offset clamped to [0,50000] (rank 7). Gateway: PII fix contact_fallback uses channel not external_id (rank 20); null store guard in handleInbound (rank 8). Test: df() helper adds Bearer auth to dashboard fetch calls (rank 23). All tests pass (ALL PASSED).
