---
key: mem-ff2192c98474f58c-769
ns: default
created: 1782483820564
updated: 1782483820564
---

gm-method (witnessed): a new integration-test assertion that drives a mutating HTTP PATCH can fail not in the code under test but on a PREREQUISITE GATE the shared fixture left the row in -- here a PATCH of a non-autonomy field 400s while the case sits in `observe` autonomy, and an earlier test in the same single real-services suite had flipped it to observe. The witness was the failure message naming the FIRST assertion (status===200), not a later one; the fix is to set the prerequisite state in the test (flip autonomy to auto first), not to weaken the assertion. Lesson: in a single shared-state real-services witness, a new test inherits whatever prior tests left the fixture in -- assert/establish the precondition explicitly rather than assuming a clean row.
