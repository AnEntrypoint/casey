---
key: mem-d66176b0daec223a-1639
ns: default
created: 1783014637843
updated: 1783014637843
---

gm-method (witnessed): a user directive to remove a 'no dead-end reply' fallback safety net can arrive mid-session while a NEW guard extending that exact safety net is being built -- the reshaping must both (a) revert the now-superseded in-flight edit and (b) remove the underlying mechanism it was extending, not just stop adding to it. Also witnessed: a 'the LLM should always work, no fallback' directive is best served by fixing the ACTUAL transport reliability (here: replacing a separate HTTP-bridged daemon process with an in-process library call, cutting real turn latency ~10-20x and removing a crash-on-timeout failure mode) rather than adding a multi-model fallback CHAIN -- a fallback chain is itself a form of 'fallback', so when a user says both 'no fallbacks' and 'it should always work', the correct read is: make the SINGLE mechanism reliable, don't add a chain of fallback mechanisms. Also witnessed: a load-bearing boolean signal (a 'degraded'/'this turn failed' flag consumed by downstream retry/queue logic) must be PRESERVED when removing the fallback TEXT that used to accompany it -- removing the whole mechanism (flag + text) can silently break queue-retry/dashboard-draft logic that keys off the flag alone; strip the composed reply, keep the signal. Also witnessed: a CJS library's named-export surface via Node's ESM interop can silently omit real module.exports keys (only a static-analysis-detected subset becomes named exports) -- when a named import returns undefined for a function you can see in module.exports, check mod.default (the full CJS exports object) before concluding the function doesn't exist.
