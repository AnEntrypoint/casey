---
key: mem-1bc68174b128c58d-1303
ns: default
created: 1781863928165
updated: 1781863928165
---

casey QUALITY-MAXIMISER PASS 4 (sha 002ce2e, main). 21 confirmed / 26 rejected from per-surface + cross-cutting audit. KEY FIXES: Concurrency: mergeCases acquires both src+tgt locks in sorted order to prevent deadlock (rank 1); autonomy re-check inside lock for merge+split (rank 12). Data integrity: mergeReport now skips blank incoming values (no clobber with empty strings) while still allowing non-blank overwrites (rank 7). Discord resilience: MAX_RETRIES -> 1-hour retry instead of silent abandon (rank 13); reconnectTimeout stored+cleared on disconnect (rank 3). Health: classifyCaseHealth returns timestamp_corrupt breach (not silent 0) when case timestamps are NaN; timestamp_corrupt added to ALL_HEALTH_TAGS (rank 9). Sweep: classify errors pushed to summary.errors[] (rank 6). Correlate: falsy || !ta || !tb removed (rank 20 -- allows timestamp 0); dead nonEmpty+safeParse deleted (rank 15); Unicode glyphs replaced with ASCII (rank 21). gateway-hooks: parseReportSafe helper extracted (rank 14); AbortController 5s timeout on Discord webhook (rank 10). findOpenCase limit 1000->2 (rank 16). dashboard: intervals stored+cleared on beforeunload (rank 18). bin/casey.js portFree: socket closed on error (rank 19). Report-parse catch blocks now log warn with caseId (rank 4). All 46 tests green.
