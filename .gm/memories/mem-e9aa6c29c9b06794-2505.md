---
key: mem-e9aa6c29c9b06794-2505
ns: default
created: 1781175005587
updated: 1781175005587
---

casey ENGINEERING-DNA QUALITY-MAXIMISER RUN 2 (AnEntrypoint/casey main). Extended .claude/workflows/casey-maximize-quality.js with two NEW cross-cutting lenses: xc-ingress-trust (WhatsApp webhook X-Hub-Signature-256 HMAC verification + public-webhook rate-limit/abuse) and xc-media-capture (audio/voice-note capture as the named open frontier). Ran it (wf_e095e9a9, 59 agents, 31 confirmed / 11 rejected). INTEGRATED 20 fixes (judgment-adjusted from the plan): SECURITY/PII -- dashboard auth matches() null-tokenBuf guard; external_id(phone) removed from findOrCreateCase error log; doctor warns WHATSAPP_APP_SECRET unset (honest unverified-webhook risk, no freddie fork). RESILIENCE -- case-sweep.js appends observation BEFORE updateCaseQuiet tag (tag is dedup key; prevents silent missing observation on partial failure); discord-receive.js drops prior ws.removeAllListeners()+terminate() on reconnect AND acked=true on HELLO (fresh socket no longer self-terminates on first heartbeat if prior left acked=false); casey.js discord adapter.start try/catch rethrow; bin/casey.js resolveCallLLM probe .catch->offline; double-SIGINT exiting guard. DATA/P12 -- inboundAudioNote() mirrors inboundImageNote: r.audio|voice|type audio/voice + audio/* attachments -> markReportFieldsIfEmpty(audio) at ingress, fill-if-empty, never blocks reply; audio field added to case-tools report schema+REPORT_KEYS and dashboard REPORT_FIELDS (Voice notes); guessLang gained af(hou op/los my/genoeg/mens) zu(yeka/hambani/umuntu) xh(umntu/hamba) cues, now EXPORTED for unit test. P1 -- pick() and splitCase subject trim-aware; appendEvent touch-failure log warn->error (NOT throw -- breaking). HONEST UI -- reply toast uses j.delivered; transition toast says note queued. SUBTRACTIVE -- removed unused hasCaseStore export, unexported makeLogger; CLI checkmark/cross + CSS /* --- x --- */ -> ASCII; resetCaseStore() called in casey.stop(). TESTS -- new voice-note-ingress + guessLang SA guardrails; test.js createCasey gets sweepIntervalMs:0 for determinism; all green. DROPPED as breaking/non-real: appendEvent-throw, discord connect return-shape change, /api/attention pagination (contract break), test external_id/adapter.sent churn (./data cleared each run so no collision), case-machine explicit-initial-state (adds coupling). STILL OPEN frontier: actual webhook-signature ENFORCEMENT lives in freddie (published, not forked) -- casey can only warn; real rate-limiting on the public webhook is unimplemented (only flagged).
