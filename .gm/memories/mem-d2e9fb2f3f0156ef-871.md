---
key: mem-d2e9fb2f3f0156ef-871
ns: default
created: 1787130480088
updated: 1787130480088
---

## Resolved mutable: mut-1787130454081

workload.js buildWorkload (lines 53-128) returns {now, stale_ms, operators: [{id,name,open_assigned,stale_claims,replies_24h,first_reply_ms_median,oldest_waiting_ms}]} -- every card field is a count/median/id/name of a ROSTERED OPERATOR, never a per-case or per-contact detail. personOwner (lines 42-45) reads only c.assignee, an operator attribution field, never a contact field. The reply-attribution loop (lines 84-91) explicitly guards against a phantom card: rosterIds.has(by) (line 87) skips any id not on the current operator roster, per the inline comment at lines 79-83 explaining this prevents a stray/former-operator id from leaking into this aggregate-only endpoint. No case ref, case id, or event content is ever placed into a card; only counts and timestamps derived from case/event data are aggregated per operator.
