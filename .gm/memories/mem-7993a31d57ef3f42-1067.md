---
key: mem-7993a31d57ef3f42-1067
ns: default
created: 1782938378694
updated: 1782938378694
---

gm-method (witnessed): a user push to strip hard-coding is a RESHAPING discovery, not additive -- when it lands mid-implementation of scaffolding, transition to PLAN and re-cut, do not keep landing the now-wrong scaffolding. The load-bearing distinction to preserve: AGENT-DECLARED state (a state machine the LLM MOVES by declaring its intent via a tool) is NOT hard-coding and stays; casey-COMPUTED conversation logic (which fact to ask next, the ack wording, keyword field extraction, a deterministic ask-ladder) IS hard-coding and goes -- the reframe is who DECIDES, not whether state is tracked. When stripping a deterministic capture floor that a stub/test double depended on, move the minimal shape logic INTO the test double (a fixture) so the single real-services witness still exercises the real tool path, and keep the safety floor to the irreducible legal/irreversible controls (opt-out/handoff) only. Ask the one load-bearing trade-off (keep vs strip the last safety net that prevents silent data loss) before the blanket strip -- the user owns that risk.
