---
key: mem-e8b3b97a7a4dc991-1041
ns: default
created: 1788863615554
updated: 1788863615554
---

casey repo, 2026-09-08: the gm `prd-add` verb fails with `error: write failed, exitCode 1` on every dispatch in this project regardless of body size, whether `.gm/prd.yml` exists or not, and with `.gm` confirmed writable (touch succeeds). Every other verb in the same session works (`instruction`, `phase-status`, `codesearch`, `cdp`, `scan_deps`). Surfaced rather than routed around; the PRD for that session lived in the commit message instead, which the served SPECIFY prose already names as the durable record. Also: `codesearch` with an absolute `root` pointing inside the project (src/dashboard/public/src) is rejected as 'not a real, existing directory the host will grant access to' -- root scoping only accepts a project root, so a narrow in-project search has to be done via the default cwd index. Also: a `cdp` body must NOT open with a `session new <id>` line when the rest of the body is JS -- the line is passed straight into the eval and throws SyntaxError: Unexpected token 'new'; use `timeout=`/`url=` prefixes plus bare JS.
