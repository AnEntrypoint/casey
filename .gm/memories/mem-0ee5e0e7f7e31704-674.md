---
key: mem-0ee5e0e7f7e31704-674
ns: default
created: 1783848855808
updated: 1783848855808
---

casey/thatcher fact (witnessed 2026-07-12): thatcher list() honors sort server-side (busybase-store.js sorts on options.sort OR spec.list.defaultSort); old casey 'thatcher ignores orderBy/order' comments were stale/false. casey keeps JS sort ONLY in event paths (listEvents via sortByCreatedStable) because event created_at is coarse unix-SECONDS with no thatcher tiebreak so pushing it down scrambles same-second replay; contacts/refs push sort down cleanly. findOpenCase must use status:{$in:openStages} allowlist NOT status:{$ne:closed} -- busybase bypasses its auto-DELETED filter when status is in the where, so $ne leaks deleted rows while the allowlist excludes them.
