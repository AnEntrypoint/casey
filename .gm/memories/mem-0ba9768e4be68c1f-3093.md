---
key: mem-0ba9768e4be68c1f-3093
ns: default
created: 1781184163668
updated: 1781184163668
---

casey GENERIC QUALITY-MAXIMISER + DEPENDENCY PASS (casey main; freddie + thatcher updated). NEW reusable .claude/workflows/maximize-quality.js: target-parameterized generalisation of casey-maximize-quality.js, args={root,projectName,context,surfaces?} (CRITICAL: the Workflow harness delivers args as a JSON STRING, parse it: const A = typeof args===string?JSON.parse(args):args). Runs Map+Audit(surfaces+6 crosscuts incl NEW xc-manifest: undeclared/unused deps, dead test scripts, exports pointing at missing files)+adversarial default-refute Verify+Synthesize. casey-maximize-quality.js left intact. FREDDIE (wf_ef1059c0, 73 agents, 49 confirmed/9 rejected) commit bb5233b on branch fix/website-latest-sdk-layout, 17 tests green: gateway/run.js + acp/server.js lifecycle-persist .catch logs not swallows; gateway message-listener catches handleInbound rejection; in-flight persist degrades not drops; send+clear in try/finally (no re-reply on send fail); sessions.js getMessages tolerates corrupt tool_calls JSON; acptoapi-bridge res.json() wrapped + process.cwd() guarded (browser-safe published export); acp/tools.js + toolsets.js filter by checkFn availability; machine.js+batch.js dropped redundant pre-check load() TOCTOU (createPersistentActor handles missing snapshot via pa.resumed). THATCHER (wf_6a179ae3, 110 agents, 81 confirmed/14 rejected) commit e4ca143 on main (rebased), 31 tests green: was BROKEN -- public CRUD (index.js) + validate.js + xstate-workflow-engine.js + notification-engine.js imported NON-EXISTENT ./lib/query-engine{,-write}.js; repointed all to ./lib/busybase-store.js (its header says it reimplements query-engine; verified exports list/get/getBy/create/update/remove/search; update drops user param, get/getBy now awaited); withTransaction->pass-through. server/server.js missing import fs (static serve threw). MANIFEST: declared previously-undeclared hyperformula+xstate (fresh install crashed), test script bun test (0 files)->node test.js, exports removed ./migration/* (no dir) + repointed ./validation/*->./src/lib/validation/*, removed 5 unused deps (@casl/ability,compression,nanoid,puppeteer,tsx). SECURITY (safe subset): google-drive GQL-escape, OAuth state compare (CSRF), debug route prod-disable, readBody 10MB/30s limits. Glyphs->ASCII. KEY LEARNINGS: (1) thatcher is a BUN project -- bun resolves jsconfig paths so @/* -> ./src/* works at its real runtime; the audit flagged @/ as broken (false positive, only breaks under node) -- REVERTED that fix. (2) thatcher test entry is node test.js NOT bun test. (3) DEFERRED real-but-behavior-flipping thatcher findings (need test coverage first, untested published paths): permission.service default-allow->deny, handleGenericCrud hardcoded-admin removal, sanitizeData strict-filter, readBody reject-bad-json, validateConfig-on-load, field-type unknown-throw. casey itself UNAFFECTED (consumes freddie/thatcher via npm not sibling source; casey test.js green). memory note that sibling thatcher is broken busybase fork is now PARTLY resolved (CRUD imports fixed) -- busybase IS a real npm dep now.
