---
key: mem-14c199133f3f3a17-2702
ns: default
created: 1781013131115
updated: 1781013131115
---

casey DOMAIN: South African animal-disease surveillance (commit 6679f6e, AnEntrypoint/casey main). casey is NOT generic case tracking -- it is for farmers + NGO field workers reporting sick/dead livestock (FMD etc) over WhatsApp in rural SA. CORE DESIGN: collect MAXIMALLY, ask MINIMALLY (one gentle question max, never interrogate); extraction is INVISIBLE to the farmer (no form-feel) while the record is kept progressively organised for the team behind the scenes -- 'people shouldnt know its assigned to do that'. AMPLIFY organisers intent, do NOT impose (removed auto priority-bump on human request; no notifiable-disease auto-escalation; priority operator-driven). DATA: thatcher.config.yml case.report = flexible JSON (species,symptoms,location,how_to_find,affected_count,dead_count,onset,suspected_disease,recent_movement,photos,identifying_traits,access_notes,farmer_available,contact_fallback,notes), non-required. case-tools.js case_report tool idempotently deep-merges partial fields each turn; case_get slims (parses report) so agent never re-asks. ONE-SHOT CAPTURE (load-bearing): after the first conversation the worker LEAVES the site and is hard to reach, so front-load the UNRECOVERABLE on-site facts (where/how_to_find/farmer_available/contact_fallback). gateway-hooks: a closing 'thanks' with a visit-critical field missing DEFERS the canned thanks-shortcut to the agent for ONE gentle ask, via an explicit closingCapture directive (mostImportantMissingField, location-first) passed into caseSystemPrompt; once-only via append-only CLOSING-NUDGE observation marker (NOT a tag -- the agent case_update clobbers tags). reportMissingVisitCritical(reportRaw) is exported. DASHBOARD (src/dashboard/server.js PAGE, browser-witnessed): reportPanel shows 15 fields ('not given yet' for blanks, falsy 0 counts as data via r[k]!=null&&trim check), a readiness line (amber 'Still missing for a visit: ...' vs green 'Has what a field visit needs' from VISIT_CRITICAL), fmtPhone (+27 82 123 4567 / 0XX), fmtTime SAST (Africa/Johannesburg + ' SAST'), NON_EN_WORDS = SA langs. SA LANGUAGES: offline tables en/af/zu/xh only (dropped es/pt/fr; live model handles Sesotho/Setswana + others); guessLang distinctive-cue scoring with English tie-break (fixed en/af ' my ' collision); LANGUAGE prompt rule strengthened so English input stays English (groq llama-3.3-70b was mis-replying in isiZulu). sim/scenarios.js = SA disease personas; stub-llm.js = disease intake (records report fields + triages). Built via adversarial-critique Workflow (waz04qvfv, 9 agents, 5 confirmed/0 rejected, all integrated). 55 tests green. Run live: boot acptoapi :4800, casey sim --real or createCasey w/ resolveCallLLM.
