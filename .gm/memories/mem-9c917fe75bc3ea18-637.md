---
key: mem-9c917fe75bc3ea18-637
ns: default
created: 1783093095917
updated: 1783093095917
---

gm-method (witnessed): gm's residual-scan and CONSOLIDATE->COMPLETE CI marker are strict about exact key names and freshness -- the .ci-validated marker file must contain a top-level "head_sha" key matching the CURRENT git HEAD exactly (not "sha" or a short sha), written fresh this session (a stale marker from an earlier HEAD fails the gate silently with a generic-looking denial). When `gh run list --json status,conclusion,headSha,workflowName` already shows a green run for the just-pushed sha, that is a legitimate witness to write into the marker -- no need to poll/watch a fresh run if CI already completed for that exact commit.
