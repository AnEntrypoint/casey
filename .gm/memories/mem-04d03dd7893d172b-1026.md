---
key: mem-04d03dd7893d172b-1026
ns: default
created: 1783114653211
updated: 1783114653211
---

gm-method (witnessed): for an open-ended 'catch any curveball' robustness ask spanning a whole conversational system, the effective multi-agent shape is: (1) one agent maps the REAL turn-flow code with exact file:line citations as shared ground truth, (2) N dimension-enumerator agents each generate 15-25 REALISTIC example user phrasings for their dimension and classify each against that ground truth -- explicitly told to only report gaps where the SYSTEM (prompt content, tool capability, deterministic guard, adapter capability) is the actual blocker, not where a capable LLM would obviously handle the phrasing fine via its own judgment, (3) adversarial default-refute verify each candidate against the actual cited file, (4) synthesize with an explicit contract-breaker rule (e.g. 'no new deterministic classifier for general understanding' in a pure-LLM system) so verifiers/synthesizer actively reject fixes that would violate the target architecture's core invariant rather than just listing every literal gap found.
