---
key: mem-1940b2d37c9d71d9-542
ns: default
created: 1783423789537
updated: 1783423789537
---

casey repo detail: postinstall runs scripts/install-hooks.mjs which points git core.hooksPath at the tracked hooks/ dir. hooks/post-merge and hooks/post-checkout touch src/casey.js after a pull/merge/checkout -- a belt-and-braces mtime bump (touch changes only the timestamp, never bytes) so a manual `git pull` also hot-reloads the supervised worker, on top of the auto-deploy fast-forward already rewriting watched source. scripts/auto-update.mjs / npm run auto-update remains as a standalone poller for a host not running under `casey up`.
