---
key: mem-6cb885eb27d508c8-1044
ns: default
created: 1782917590795
updated: 1782917590795
---

gm-method (witnessed): a capability the docs/architecture-mandate DESCRIBE may never be WIRED -- a whole toolset/feature can exist in one module yet be absent at runtime because the host's plugin-discovery never registers it (the file lacks the discovered entrypoint, or a duplicate lower-capability version shadows it). Symptom: a per-user/scoped operation silently degrades to an unscoped global one (e.g. a per-worker 'my cases' returns the whole fleet, over-exposing other rows). A fan-out scenario sweep catches it: drive the feature with a fresh isolated harness and assert the SCOPED/PII-free behavior, then when it fails inspect what tools/handlers are ACTUALLY registered (dump the live registry), not what the source defines. Fix options, cheapest-first: add the missing tools to the ALREADY-registered module (reusing its ctx), rather than registering a second overlapping toolset that collides on shared names. A LIST spanning rows the asker may not own must project each row through a PII-free whitelist, never the raw full record.
