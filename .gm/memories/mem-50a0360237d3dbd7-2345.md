---
key: mem-50a0360237d3dbd7-2345
ns: default
created: 1781087432318
updated: 1781087432318
---

casey REUSABLE QUALITY WORKFLOW (commit b857c41, AnEntrypoint/casey main). .claude/workflows/casey-quality-audit.js is a reusable multi-agent audit: one auditor per surface (gateway-hooks, case-data, dashboard, orchestration, sim-tests) through the synthesized-engineering-dna 12 principles, pipelined into adversarial verifiers (default-refute), returns confirmed net-improving non-breaking fixes sorted by severity. Parameter-free; optional args = surface-key subset. Invoke via Workflow{scriptPath:'C:/dev/casey/.claude/workflows/casey-quality-audit.js'} (the saved-workflow-by-name registry did NOT resolve .claude/workflows in this env -- use scriptPath). FINDING_SCHEMA + VERDICT_SCHEMA self-contained. NOTE: .gitignore was changed to '.claude/*' + '!.claude/workflows/' so the workflow is tracked while .claude settings stay ignored (a plain '!.claude/workflows/' under '.claude/' does NOT work -- git won't descend into an excluded dir; must exclude by glob .claude/* not the dir). First run (wf_a4b06094-967, 21 agents): 12 confirmed/4 rejected. FIXES INTEGRATED: (gateway-hooks.js) STATUS intent moved from substring hit() to whole-word live() so 'updated'/'stopped' in a report reach the agent not a canned shortcut; multi-word negation guard via phraseLive position-scan ('please dont leave me alone' != stop); prompt report filter !=null. (case-store.js) case_report now atomic mergeReport(caseId,incoming) under _withLock(channel|external_id) -- concurrent agent turns cannot clobber report fields (witnessed 6 parallel merges none-lost); thatcher ignores orderBy so listCases/listEvents(asc)/listEventsPage(desc+JS-page)/_nextRef/_createReload sort in JS (byCreatedAsc/Desc, recencyKey helpers) and dropped the dishonest ignored orderBy params. (dashboard) /api/cases validates ?status via store.getValidStatuses(); /api/health bounds model(100)/url(200); PAGE scrubs ?token= via history.replaceState + sends X-Casey-Token HEADER not URL query (browser-witnessed urlHasToken=false, header-auth 200, bare 401). (discord-receive.js) exponential backoff 3s..30s + MAX_RETRIES 8 + reset on HELLO, replacing tight 3s loop. (casey.js) callLLM removed from Gateway ctor (P4, case handler owns it). REJECTED: full token->cookie auth rework (breaking; did URL-scrub mitigation). test.js guardrails added for all 3 bug classes. Tests green.
