---
key: mem-1238fc30c284e3f4-1945
ns: default
created: 1780944566697
updated: 1780944566697
---

casey maximal low-tech-literacy uplift (commit b9b7873, AnEntrypoint/casey main). Built from a 10-agent Workflow fan-out (5 design + 5 adversarial critique across operator-ux/contact-ux/intent/sim/notify); integrated the critique-corrected code. OPERATOR side (src/dashboard/server.js PAGE): 'Needs you now' triage inbox pinned top with deterministic attnScore (needs-human>stale-waiting>observe/assisted, opted-out=0), per-row plain reason, calm empty state; per-case cannedReplies (tap-to-fill, [] for opted-out) + needs-human/opted-out todoHint rules; contactMaybeNonEnglish language warning (NOTE: PAGE is a template literal so regex needs  escaped and no literal combining chars); loud handoff banner (webaudio chime+title flash, once per case via handoffSeen localStorage); attn() now includes needs-human; operator reply route clears needs-human tag. CONTACT side (src/gateway-hooks.js): stageNote+makeTransitionNotifier proactive notes on OPERATOR transitions only (skips agent/opted-out/no-op/dup; internal+closed silent); case-store.js transition() skips no-op moves + fires onTransition hook; casey.js wires sendReply(caseRow,text)+notifyHandoff. Hardened detectContactIntent: word-boundary STOP/HUMAN with negation guards + STOP_EXCLUDE/HUMAN_EXCLUDE phrases, forgiving substring for read-only STATUS/HELP/THANKS/GREETING (short keys<=3 chars whole-word only), zu/xh/ar/hi/pt langs, ??? help via normalize ?-collapse, greeting/thanks intents; yes/no/bare-numbers deliberately fall through to agent (they answer its question); discordHandoffNotifier gated !alreadyFlagged. test.js 42 green incl OPERATOR FLOW, END-TO-END LIFECYCLE, PROACTIVE NOTE, intent guards, opt-out/observe respect; 2 new personas (full-lifecycle, false-positive-guard). Dashboard client edits browser-witnessed (caught+fixed 2 real client syntax bugs: char-class range crash, ->backspace). Whole source converted to ASCII (gateway-hooks normalize uses ̀-ͯ).
