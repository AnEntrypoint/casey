---
key: mem-7a54a3eb72e649c2-1178
ns: default
created: 1782998216691
updated: 1782998216691
---

gm-method (witnessed): a live user bug report quoting the EXACT output text is the highest-value witness for a 'safety fallback is a silent no-op' bug class -- grep the codebase for that literal string first, before reasoning about prompt quality or model behavior. Found instance: a degraded-turn fallback constant was byte-identical (after the guard's own normalization) to the exact stock-ack string an upstream content guard rejects, so the guard fired, blanked the reply, then handed back the identical rejected text -- the live symptom (a canned-sounding reply on a bare greeting) was reported by the user as 'the model is being dumb' but the actual defect was in the deterministic scrub layer, not the model. Fix pattern: (1) grep the reported literal text to its defining constant, (2) check every guard/normalizer that could touch that same string for a byte-identical round-trip, (3) make the fallback text branch on real state (e.g. whether anything was actually recorded yet) rather than being one static string, (4) add a permanent structural regression assertion (guard(fallback()) === false) so future wording edits cannot silently reintroduce the same collision.
