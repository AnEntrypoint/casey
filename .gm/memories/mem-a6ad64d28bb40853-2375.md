---
key: mem-a6ad64d28bb40853-2375
ns: default
created: 1780999030672
updated: 1780999030672
---

casey<->acptoapi LIVE integration + acptoapi engineering-DNA pass (casey a3aba51, freddie 740738b on fix/website-latest-sdk-layout, acptoapi b0ec6c6). LOAD-BEARING: casey only ever ran stub/null LLM -- freddie's acptoapi bridge (src/agent/acptoapi-bridge.js, {messages,tools}->{content,tool_calls}) was NEVER wired in. Fixed: freddie/src/index.js now exports acptoapiCallLLM/acptoapiReachable/getAcptoapiUrl (the bridge subpath was blocked by exports map); casey/src/llm.js resolveCallLLM picks stub|acptoapi|none by honest precedence (CASEY_STUB_LLM, else isReachable probe, else null->canned fallback); bin/casey.js up+sim --real use it + plain-words AI-helper banner. acptoapi runs live on :4800 (node bin/acptoapi.js --port 4800; .env has groq/cerebras/sambanova/etc keys; PROVIDER_ORDER groq-first; claude/haiku resolves to a real model). LIVE-CRUCIBLE exposed real bugs ALL fixed+witnessed: (1) runTurn timeoutMs unset=freddie 30s default too tight for cold start -> casey now 120s (CASEY_LLM_TURN_TIMEOUT_MS); (2) fallback+intentReply+plainStatus were English-only so a Spanish contact got English on timeout/thanks -> added fallbackReply+guessLang(distinctive-cue scoring, English tie-break for es<->pt collision)+localized FALLBACK/INTENT/STATUS tables es/pt/fr/af/zu/ar/hi in gateway-hooks.js; (3) sim printed arbitrary listCases()[0] (thatcher ignores orderBy) -> now prints reply.caseId's case. Dashboard: /api/health + llmStatus option + #aihealth pill (browser-witnessed green online). acptoapi DNA pass (Workflow w3839od05, 32 agents, 26 confirmed/1 rejected, adversarially verified): lib/server.js readBody 400-on-bad-JSON + outer catch honours e.status + no stack leak + async dispatches awaited (was unhandledRejection drop) + handleBrandChat headersSent guard; circuit-breaker isOpen full-reset after cooldown (was pinned maxFailures); sampler/keyring clear lastFailedAt; model-resolver age>=0 clock-skew guard; response-cache set()-time JSON validation; toResponse echoes model; removed dead validate-prd.js+metrics.snapshot+config.interpolateEnv+hello.txt. Witnessed: malformed->400, valid chat ok, model echoed, concurrent es/pt/en burst+dup deduped each replied in own language, 58 tools delivered. acptoapi has NO npm test; casey test.js 48 green. To run live: boot acptoapi on 4800, then casey sim --real or createCasey with resolveCallLLM().callLLM.
