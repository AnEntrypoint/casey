---
key: mem-33d2bcfa596c62cb-1842
ns: default
created: 1781092659477
updated: 1781092659477
---

casey SECOND QUALITY PASS (commit 0823f4c, AnEntrypoint/casey main): reached the surface the first casey-quality-audit did NOT cover -- the media ingress path. FINDING: a received animal photo (the highest-value one-shot on-site artifact, unrecoverable after the worker leaves) was only labelled into the inbound event TEXT by describeMedia ('an image') and left to the agent turn to notice/record via case_report -- which it may skip on a media-only message, so the picture could never become structured operator-visible state. FIX: gateway-hooks.js inboundImageNote(msg) detects a REAL image (raw.image | raw.type==='image' | attachment image/* content-type) and excludes stickers/audio/generic; the handler fills report.photos at ingress (after dedup-pass, before the autoRespond gate, try/catch so it never blocks the reply) and logs a 'PHOTO RECEIVED' observation only when it filled. case-store.js markReportFieldsIfEmpty(caseId,fields) fills ONLY blank fields under the per-conversation lock + observe guard -- fill-if-empty makes clobbering an agent-recorded richer photo note structurally impossible (P6), distinct from mergeReport (incoming-wins). Guardrail test drives a media-only WhatsApp inbound (raw.type==='image') through makeCaseHandler with callLLM=null/autoRespond=false, asserting: photos set at ingress with no agent turn; a later photo does NOT clobber the agent's richer note; a sticker does NOT set photos. NOTE for future passes: localization (guessLang tie->en, fallbackReply localized tails af/zu/xh) and intent detection (live/phraseLive negation guards) are already mature; the media path was the genuine gap. Other unconsidered frontiers still open if a third pass is wanted: webhook signature verification on the WhatsApp ingress, rate-limit/abuse on the public webhook, audio/voice-note transcription capture.
