---
key: mem-712dc1fb37af013f-749
ns: default
created: 1782671268668
updated: 1782671268668
---

casey once-per-field DEFECT (gateway-hooks.js): empty-model branch (800-841) and INTAKE-DRIVE gate (860-876) BOTH run in one turn (gate is not else-of empty branch). Empty branch appends FALLBACK-ASK:location + sets text=location-ask; appendEvent persists to thatcher so gate's listEvents(861) now sees location asked; nextUnaskedMissingField returns species; gate appends FALLBACK-ASK:species and overwrites text with species-ask (driven!==text true). Result: location recorded asked-once but its reply text DISCARDED, never delivered. Field is BURNED without being asked -> once-per-field guard later skips it forever. Fix: skip INTAKE-DRIVE gate when empty branch already drove intake (guard with a flag e.g. droveIntake, or make gate `else if`).
