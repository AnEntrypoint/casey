---
key: mem-710b3e7bfa48a187-1080
ns: default
created: 1783420748217
updated: 1783420748217
---

## Resolved mutable: mut-attn-snooze-atrisk-mismatch

attn.js attnScore() (line ~101-116): const snooze = snoozedUntil(c); if (snooze && Number.isFinite(now) && now < snooze) { if (!tags.includes('needs-human')) return 0 } -- a snoozed non-needs-human case scores 0 and is excluded from rankAttention's ranked/sorted items list. attn.js atRiskCount() (line ~91-98): function atRiskCount(cases, now, targetMs) { for (const c of cases||[]) { const w = waitAgeMs(c, now); if (w != null && w >= targetMs) n++ } return n } -- waitAgeMs() (line ~81-87) calls waitingOnUs() (line ~66-73) which checks status/opted-out/WAITING_TAGS only, NEVER snoozedUntil. CONFIRMED REAL: a case that is snoozed (and not needs-human) is invisible in the inbox item list (attnScore=0) but STILL counts toward the atRisk number rankAttention returns (since atRiskCount never checks snooze) if its wait age exceeds slaTargetMs. The team header's at-risk count can be higher than the number of at-risk-looking items an operator can actually see/act on in the list, undermining trust in the SLA clock number.
