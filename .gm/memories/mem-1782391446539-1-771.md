---
key: mem-1782391446539-1-771
ns: default
created: 1782391447845
updated: 1782391447845
---

## Resolved mutable: mut-freddie-theresa-global-state

../freddie/src/db.js:6-8 holds module-level `let _db = null; let _dbPromise = null` lazily primed from getFreddieHome()/state/sessions.db -- a process-global singleton. An in-process cache-bust reload would fork this handle (same hazard as thatcher's cwd-bound store); a child-worker respawn cleanly resets it (fresh module graph re-runs the lazy init and reopens the SAME file by home dir, so no data loss). theresa is NOT a sibling here (ls ../theresa empty); the real siblings are ../freddie and ../anentrypoint-design with thatcher at node_modules/thatcher. CONFIRMS: respawn is the clean reset; freddie's session db survives respawn by reopening its home-keyed file, identical durable-file boundary to thatcher.
