---
key: mem-3638cfb686faceb1-618
ns: default
created: 1783114655512
updated: 1783114655512
---

gm-method (witnessed): when adding buffer-and-replay logic to a message handler that a caller binds with .bind(this) for platform-adapter lookups inside the handler body, renaming the core logic to an inner function and wrapping it with a thin outer function requires the wrapper to explicitly forward `this` via .call(this, ...) at every call site (both the initial invocation and any recursive replay) -- a plain arrow-function wrapper or an unguarded call silently loses the bound context and breaks adapter/platform lookups inside the core logic with no obvious error at edit time, only a runtime null-deref later.
