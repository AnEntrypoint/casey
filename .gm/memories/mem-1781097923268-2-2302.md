---
key: mem-1781097923268-2-2302
ns: default
created: 1781097926980
updated: 1781097926980
---

casey CASE CORRELATION / MERGE / SPLIT (commit c89427a, AnEntrypoint/casey main). casey now intelligently groups same-outbreak reports and separates distinct ones, and can post-fix wrong grouping. NEW src/correlate.js (PURE, no I/O, unit-tested): correlationScore(a,b)->{score,reasons} over same-contact, fallback-number cross-link (normPhone keeps last 9 digits so +27 82 123 4567 == 082 123 4567), location/how_to_find token Jaccard (NFD accent-strip + STOP words like farm/plaas/area), species, symptoms+suspected_disease; TIME only amplifies an existing content match and can NEVER manufacture one (test asserts same-instant-nothing-in-common == 0). suggestLinks(target,others,threshold=0.35) ranks; looksLikeNewOutbreak(openReport,incoming) is the conservative ingest signal (different location AND species, both present). case-store.js: updateEvent (re-point case_id primitive); mergeCases(source,target,user,{reason}) under TARGET lock = LOSSLESS (events re-pointed never deleted) + IDEMPOTENT (guarded by the 'merged' tag, NOT event count -- the redirect note + _forceClose transitions remain on the source) + fill-if-empty report merge (target canonical) + tag union + source closed via _forceClose (multi-hop to 'closed' as SYSTEM_USER admin since closed requires_role operator/admin); splitCase(source,eventIds,{subject,reason}) under source lock = create new case (EMPTY report, honest -- no faked partition) + re-point events + link notes on both, guards empty/all-events/unknown-event. case-tools.js: case_link_suggestions, case_merge, case_split (all observe-guarded). gateway-hooks.js system prompt: private 'KEEP REPORTS CORRECTLY GROUPED' block (agent splits a clear second outbreak, merges a clear same one, quietly -- invisible to farmer). dashboard server.js: GET /api/cases/:id/suggestions + POST /api/cases/:id/merge {into} + a 'Possibly the same case' PAGE panel (loadDuplicateSuggestions) with reasons + merge button (BROWSER-WITNESSED browser-20: panel renders, merge folds 1 event, suggestion gone after). NOTE: case.status enum is fixed to workflow stages -- could NOT add a 'merged' status without it being a stage; used tags+close+redirect-note instead (worked within the constraint). Split provenance note text is 'Split out of <ref>' (not 'Split from'). All tests green.
